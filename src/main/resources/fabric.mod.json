{
	"schemaVersion": 1,
	"id": "${id}",
	"version": "${version}",
	"name": "${name}",
	"description": "${description}",
	"authors": [
		"${author}"
	],
	"contact": {
		"homepage": "https://github.com/Ayydxn/BetterFletching",
		"sources": "https://github.com/Ayydxn/BetterFletching/issues"
	},
	"license": "${license}",
	"icon": "assets/better-fletching/${icon_file}",
	"environment": "*",
	"entrypoints": {
		"main": [
			"com.ayydxn.betterfletching.BetterFletchingMod"
		]
	},
	"mixins": [
		"better-fletching.mixins.json"
	],
	"depends": {
		"minecraft": "~${minecraft_version}",
		"fabricloader": ">=${fabric_loader_version}",
		"fabric-api": "${fabric_api_version}"
	}
}